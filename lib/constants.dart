import 'package:flutter/material.dart';

const Color kPrimaryColor = Color(0xFFFF7643);
const Color kPrimaryLoghtColor = Color(0xFFFFECDF);
const Gradient kPrimaryGradientColor = LinearGradient(
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
    colors: [Color(0xFFFFA53E), Color(0xFFFF7643)]);
const Color kSecondaryColor = Color(0xFF979797);
const Color kTextColor = Color(0xFF757575);

const kAnimationDuration = Duration(microseconds: 200);
